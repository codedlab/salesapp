# This is a basic workflow to help you get started with Actions final test

name: deploy-expiration

on:
  push:
    branches:
      - main
    paths:
      - "expiration/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd expiration && docker build -t ceejaysay/expiration .
      - run: docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      - run: docker push ceejaysay/expiration
      - uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
      - run: cd expiration && docker build -t ${{ secrets.REGISTRY_NAME }}/expiration .
      - run: doctl registry login --expiry-seconds 1200
      - run: docker push ${{ secrets.REGISTRY_NAME }}/expiration
      - run: doctl kubernetes cluster kubeconfig save ticketing
      - run: kubectl rollout restart deployment -n expiration-depl
